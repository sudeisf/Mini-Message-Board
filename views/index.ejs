<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

   
        <ul>
            <% data.map((message ,index) => { %>
                <li>
                    <p>User <%= index %> : <%= message.user_name %></p>
                    <h3> message: <%= message.message_text %></h3>
                    <p>recived :<%= message.recived %></p>
                    <a href="/message/message/<%= message.id %>">Open</a>
                </li>
                <% }) %>

        </ul>
    
</body>
</html>